<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Navigation.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Navigation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBox Header="Username:" Width="200" Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <PasswordBox Header="Password:" Width="200" Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <CheckBox IsChecked="{Binding IsRememberme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Content="Remember me"/>
        <Button Content="Login" Command="{Binding LoginCommand}" Width="200"/>
    </StackPanel>
</Page>
