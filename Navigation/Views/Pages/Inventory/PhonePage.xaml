<Page
    x:Class="Navigation.Views.Inventory.PhonePage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhoneSelling.ViewModel.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:PhoneSelling.Data.Models"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <Pivot x:Name="MainPivot">
            <!-- PivotItem for displaying all product series groups -->
            <PivotItem Header="Groups">
                <StackPanel Margin="10">
                    <ListView ItemsSource="{x:Bind _viewModel.SeriesGroups}" 
                              SelectionMode="Single" 
                              SelectionChanged="GroupsList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="data:ItemGroup">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <TextBlock Text="{x:Bind ItemGroupName}" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text=" - " FontSize="20" Margin="5,0"/>
                                    <TextBlock Text="{x:Bind Category}" FontSize="20"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </PivotItem>

            <!-- PivotItem for displaying all items (flattened) -->
            <PivotItem Header="Items">
                <StackPanel Margin="10">
                    <ListView ItemsSource="{x:Bind _viewModel.AllItems}" 
                              SelectionMode="Single" 
                              SelectionChanged="ItemsList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="data:Item">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <TextBlock Text="{x:Bind ItemName}" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </PivotItem>

            <!-- PivotItem for displaying all variants with DisplayName (flattened) -->
            <PivotItem Header="Variants">
                <StackPanel Margin="10">
                    <ListView ItemsSource="{x:Bind _viewModel.AllVariantDisplays}" 
                              SelectionMode="Single" 
                              SelectionChanged="VariantsList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:VariantDisplay">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <TextBlock Text="{x:Bind DisplayName}" FontSize="16" FontWeight="Bold"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
