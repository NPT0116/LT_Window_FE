<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Navigation.Views.PhonePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Navigation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:PhoneSelling.Data.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView ItemsSource="{Binding Phones}" Margin="20">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Phone">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <!-- Bind properties of Phone. Adjust property names as needed -->
                        <TextBlock Text="{x:Bind Name}" FontWeight="Bold" Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Bind Price}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
